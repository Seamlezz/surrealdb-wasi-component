# SurrealDB WIT Package

This directory defines the `seamlezz:surrealdb@0.1.0` package.

The package exposes one async interface, `call.query`, with CBOR encoded parameters and results.

Publish with Taskfile targets from repository root.
